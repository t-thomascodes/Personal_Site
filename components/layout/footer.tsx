export default function Footer() {
  return (
    <footer className="relative z-[1] px-10 py-8 flex justify-between text-[0.65rem] text-[var(--faint)] tracking-wide border-t border-white/[0.04] max-md:flex-col max-md:gap-2 max-md:text-center max-md:px-6">
      <span>© 2026 tony thomas</span>
      <span>boston — and everywhere else</span>
    </footer>
  )
}
